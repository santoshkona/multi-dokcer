sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t santoshkona/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true santoshkona/react-test npm test

after_success:
  - docker build -t santoshkona/multi-client ./client
  - docker build -t santoshkona/multi-nginx ./nginx
  - docker build -t santoshkona/multi-server ./server
  - docker build -t santoshkona/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push santoshkona/multi-client
  - docker push santoshkona/multi-nginx
  - docker push santoshkona/multi-server
  - docker push santoshkona/multi-worker
